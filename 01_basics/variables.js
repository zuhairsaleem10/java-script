const accountID = "f2023065298";
let accountBalance =10000;
var accountType = "Salary";
city=Bhakkar;
console.table([accountID, accountName, accountBalance, accountType]);
// prefer not to use var because of its function scope and hoisting issues. Instead, use let and const for better readability and maintainability of code.
